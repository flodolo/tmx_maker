language: python
install:
  - pip install flake8
  - pip install -r requirements.txt
script:
  - python -m unittest discover
  - flake8 .
matrix:
  fast_finish: true
  include:
    - python: "2.7"
    - python: "3.5"
    - python: "3.6"
    - python: 3.7
      dist: xenial
      sudo: true
